<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seoulauction.renewal.mapper.aws.FooterMapper">
    <!--FAQ -->
    <select id="selectFaqs" parameterType="CommonMap" resultType="CommonMap">
        SELECT
            *
        FROM
            FAQS
        WHERE
            IS_OPEN = 'Y'
            AND STATUS = 'A'
        ORDER BY
            CREATED_AT desc
        limit
            #{offset} , #{size}
    </select>
    <!--언론 보도 -->
    <select id="selectContentPress" parameterType="CommonMap" resultType="CommonMap">
        SELECT
            *
        FROM
            CONTENT_PRESS
        WHERE
            STATUS = 'A'
        ORDER BY
            CREATED_AT desc
        limit
            #{offset} , #{size}
    </select>
    <!--채용 공고 -->
    <select id="selectRecruit" parameterType="CommonMap" resultType="CommonMap">
        SELECT
            *
        FROM
            RECRUIT
        WHERE
            STATUS = 'A'
            AND IS_OPEN = 'Y'
            AND NOW() >= START_AT
            AND NOW()  <![CDATA[ <= ]]> END_AT
        ORDER BY
            CREATED_AT desc
        limit
            #{offset} , #{size}
    </select>
    <!--공지 사항 -->
    <select id="selectNotice" parameterType="CommonMap" resultType="CommonMap">
        SELECT
            *
        FROM
            NOTICE
        WHERE
            STATUS = 'A'
            AND IS_OPEN = 'Y'
        ORDER BY
            CREATED_AT desc
        limit
            #{offset} , #{size}
    </select>
</mapper>