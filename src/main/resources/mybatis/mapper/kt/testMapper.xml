<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seoulauction.renewal.mapper.kt.KTTestMapper">
	<select id="selectArtist" parameterType="CommonMap" resultType="CommonMap">
		SELECT
		    *
		FROM
			ARTIST a
		LIMIT
			#{offset} , #{size}
	</select>
</mapper>